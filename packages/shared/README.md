# Umbil shared package

Types, API client helpers, and clinical constants shared by the Next.js web app and Expo mobile app.

```ts
import { createApiClient, WORKFLOW_TOOLS, API_PATHS } from "@umbil/shared";
```

Keep domain labels and endpoint paths here so web and mobile stay aligned.
